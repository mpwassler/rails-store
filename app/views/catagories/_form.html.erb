<%= form_with(model: catagory, local: true) do |form| %>
  <% if catagory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(catagory.errors.count, "error") %> prohibited this catagory from being saved:</h2>

      <ul>
      <% catagory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field field-group">
    <%= form.label :title %>
    <%= form.text_field :title, id: :catagory_title %>
  </div>

  <div class="field field-group">
    <%= form.label :slug %>
    <%= form.text_field :slug, id: :catagory_slug %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
