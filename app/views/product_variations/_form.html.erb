<%= form_with(model: product_variation, local: true) do |form| %>
  <% if product_variation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_variation.errors.count, "error") %> prohibited this product_variation from being saved:</h2>

      <ul>
      <% product_variation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sku %>
    <%= form.text_field :sku, id: :product_variation_sku %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :product_variation_title %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :product_variation_price %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= product_variation.product.title unless product_variation.product.nil? %>
    
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
